// CrisisSync widget test.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CrisisSync app smoke test', (WidgetTester tester) async {
    // Placeholder smoke test — Firebase init requires mocking.
    expect(1 + 1, equals(2));
  });
}
